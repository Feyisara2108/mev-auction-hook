# Compiler files
cache/
out/

# Broadcast logs (all chains)
broadcast/

# Dotenv files
.env
.env.*

# Git submodule config (kept local only)
.gitmodules

# Node modules
node_modules/

# Forge dependency libs (cloned locally; restored with forge install)
lib/

# Frontend build artifacts
frontend/.next/

# Un-ignore frontend source lib (ABI, constants, wagmi config)
!frontend/src/lib/
!frontend/src/lib/**